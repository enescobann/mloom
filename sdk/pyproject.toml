[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mloom-sdk"
version = "0.1.0"
description = "Python SDK for logging LLM metrics"
dependencies = [
    "requests",
    "openai" # rely user for it or add more providers. later's choice
]
